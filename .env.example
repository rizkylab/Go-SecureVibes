# Environment variables for Docker Compose

# Scanner configuration
SCAN_TARGET=.
FORMAT=both
SEVERITY=low
VERSION=dev

# Output configuration
OUTPUT_DIR=./reports

# CI/CD configuration
CI_MODE=true
FAIL_ON=high
VERBOSE=true

# Optional DAST configuration
# INCLUDE_DAST=true
# TARGET_URL=http://localhost:8080
